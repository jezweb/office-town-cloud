# Scripts

Deployment + operations scripts for Office Town Cloud.

Planned (to be implemented during Phase 1+):

- `deploy-all.sh` — deploy every worker in the right order
- `seed-substrate.sh` — initial R2 + D1 + Vectorize setup
- `verify-deployment.sh` — smoke-test the deployment
- `migrate-from-goanna.sh` — import a goanna-shaped substrate as a starting wiki
