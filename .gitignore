.DS_Store
node_modules/
.wrangler/
dist/
.dev.vars
*.log
.env
.env.local

# .jez/ is local-only working space (plans, audits, scripts, screenshots).
# This is a PUBLIC repo — keep the whole tree off GitHub.
.jez/
