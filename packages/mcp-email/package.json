{
	"name": "@office-town/mcp-email",
	"version": "0.1.0",
	"private": true,
	"description": "MCP server for Office Town email — inbound via Cloudflare Email Routing, outbound via SMTP2Go or Cloudflare Email Sending.",
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"hono": "^4.6.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250101.0",
		"typescript": "^5.6.0",
		"wrangler": "^3.85.0"
	}
}
