// Office Town Cloud — shared types, schemas, and utilities
// This package is the type contract between workers and MCP adapters.

// Placeholder — concrete types added during Phase 1 (repo bootstrap).
export const VERSION = '0.1.0';
